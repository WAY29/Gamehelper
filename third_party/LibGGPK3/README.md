Vendored [LibGGPK3](https://github.com/aianlinb/LibGGPK3) (AGPL-3.0) for reading PoE2 `Content.ggpk`.

`oo2core.dll` is **not** in git. Put it next to `GameHelper.exe` (or in `GameHelper/native/` before build).
